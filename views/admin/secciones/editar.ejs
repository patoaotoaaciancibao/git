<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Editar Sección: <%= seccion.titulo %></title>
  <link href="/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
</head>
<body>

<%- include('../../partials/navbar') %>

<div class="container py-5">
  <h1 class="mb-4">Editar Sección</h1>

  <% if (messages.error) { %>
    <div class="alert alert-danger"><%= messages.error %></div>
  <% } %>
  <% if (messages.success) { %>
    <div class="alert alert-success"><%= messages.success %></div>
  <% } %>

  <form action="/auth/curso/<%= idCurso %>/editar-seccion/<%= seccion.id_seccion %>" 
        method="POST" 
        enctype="multipart/form-data">
    
    <!-- Título -->
    <div class="mb-3">
      <label for="titulo" class="form-label">Título</label>
      <input type="text" id="titulo" name="titulo" class="form-control" 
             value="<%= seccion.titulo %>" required>
    </div>

    <!-- Portada -->
    <div class="mb-3">
      <label class="form-label">Portada actual</label><br/>
      <% if (seccion.portada) { %>
        <img src="/secciones/<%= seccion.portada %>" 
             alt="Portada" class="img-thumbnail mb-2" style="max-width:200px;">
      <% } else { %>
        <p class="text-muted">No hay portada.</p>
      <% } %>
      <input type="file" name="portada" accept="image/*" class="form-control">
      <small class="text-muted">Si subes una nueva, reemplazará la anterior.</small>
    </div>

    <!-- Vídeo -->
    <div class="mb-3">
      <label class="form-label">Vídeo asociado</label><br/>
      <% if (seccion.video) { %>
        <video width="100%" controls class="mb-2">
          <source src="/videos/<%= idCurso %>/<%= seccion.video %>" type="video/mp4">
          Tu navegador no soporta vídeo.
        </video>
        <form action="/auth/curso/<%= idCurso %>/eliminar-video/<%= seccion.id_seccion %>" 
              method="POST" class="d-inline mb-3">
          <button class="btn btn-sm btn-danger">
            <i class="fas fa-trash-alt"></i> Eliminar vídeo
          </button>
        </form>
      <% } else { %>
        <p class="text-muted">No hay vídeo subido.</p>
      <% } %>
      <input type="file" name="video" accept=".mp4,.webm" class="form-control">
      <small class="text-muted">MP4 o WEBM, máx. 3 MB. Si subes uno nuevo, reemplazará el anterior.</small>
    </div>

    <!-- Botones -->
    <div class="d-flex gap-2">
      <button type="submit" class="btn btn-primary">
        <i class="fas fa-save"></i> Guardar cambios
      </button>
      <a href="/auth/curso/<%= idCurso %>" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Volver al curso
      </a>
    </div>
  </form>
</div>

<%- include('../../partials/footer') %>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
