<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Perfil</title>
    <!-- Incluyo tus CSS, como Bootstrap y Font Awesome para los iconos -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body { background-color: #f8f9fa; }
        .profile-picture {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #dee2e6;
        }
    </style>
</head>
<body>
    <!-- 
      CORRECCIÓN: Se quita el guion bajo '_' del nombre del archivo.
      Ahora la ruta es correcta: '../partials/navbar'
    -->
    <%- include('../partials/navbar') %>

    <div class="container mt-5 mb-5">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6">
                <div class="card shadow-sm">
                    <div class="card-header bg-dark text-white">
                        <h3 class="mb-0">Mi Perfil</h3>
                    </div>
                    <div class="card-body text-center p-4">
                        
                        <% if (locals.success) { %>
                            <div class="alert alert-success">¡Foto de perfil actualizada con éxito!</div>
                        <% } %>
                        <% if (locals.error) { %>
                            <div class="alert alert-danger"><%= error %></div>
                        <% } %>
                        <% if (locals.passwordChanged) { %>
                            <div class="alert alert-success">¡Contraseña actualizada con éxito!</div>
                        <% } %>

                        <img src="/profile/<%= usuario.foto_perfil || 'default-avatar.png' %>" alt="Foto de Perfil" class="profile-picture mb-3">

                        <h4><%= usuario.nombre %></h4>
                        <p class="text-muted mb-4"><%= usuario.email %></p>

                        <hr>

                        <h5 class="mt-4">Cambiar Foto de Perfil</h5>
                        <form action="/auth/perfil/upload" method="POST" enctype="multipart/form-data" class="mt-3">
                            <div class="input-group">
                                <input type="file" name="foto" class="form-control" id="profileImage" required>
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-upload me-2"></i>Subir
                                </button>
                            </div>
                        </form>
                        
                        <a href="/auth/cambiar-contrasena" class="btn btn-outline-secondary mt-3 w-100">
                           <i class="fas fa-key me-2"></i> Cambiar Contraseña
                        </a>

                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
