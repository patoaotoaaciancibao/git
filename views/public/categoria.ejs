<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title><%= categoria %> - Cursos</title>
    <%- include('../partials/head') %>
</head>
<body>
<%- include('../partials/navbar') %>

<div class="container py-5">
    <h1 class="mb-4 text-center"><%= categoria %> - Cursos</h1>
    <p class="text-center text-muted">Total de cursos en esta categoría: <strong><%= totalCursos %></strong></p>

    <% if (cursos.length === 0) { %>
        <div class="alert alert-info text-center mt-4">
            No hay cursos registrados en esta categoría aún.
        </div>
    <% } else { %>
        <div class="row g-4">
            <% cursos.forEach(curso => { %>
                <div class="col-md-4">
                    <div class="card h-100 shadow-sm rounded-4">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title mb-3"><%= curso.nombre %></h5>
                            <p class="card-text"><%= curso.descripcion || 'Sin descripción disponible.' %></p>
                            <p class="text-muted small mb-2">
                                <strong>Profesor:</strong> <%= curso.nombre_profesor ? curso.nombre_profesor + ' ' + curso.apellido_profesor : 'No asignado' %>
                            </p>
                            <p class="text-muted small mb-2">
                                <strong>Inscriptos:</strong> <%= curso.cantidad_inscriptos %>
                            </p>

                            <!--<a href="#" class="btn btn-primary mt-auto disabled">Más información próximamente</a>-->
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    <% } %>
    <div class="text-center my-4">
        <a href="/public/home" class="btn btn-outline-primary px-3 py-2">
            <i class="fas fa-arrow-left me-2"></i>Volver al Inicio
        </a>
    </div>

</div>

<%- include('../partials/footer') %>
</body>
</html>
