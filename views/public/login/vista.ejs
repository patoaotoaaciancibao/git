<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8">
    <title>Panel del Usuario - eLEARNING</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

    <!-- Favicon -->
    <link href="/img/favicon.ico" rel="icon">

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&display=swap" rel="stylesheet">

    <!-- Icon Fonts -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="/lib/animate/animate.min.css" rel="stylesheet">
    <link href="/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">

    <!-- Customized Bootstrap Stylesheet -->
    <link href="/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="/css/style.css" rel="stylesheet">
</head>

<body>
<!-- Spinner Start -->
<div id="spinner" class="show bg-white position-fixed translate-middle w-100 vh-100 top-50 start-50 d-flex align-items-center justify-content-center">
    <div class="spinner-border text-primary" style="width: 3rem; height: 3rem;" role="status">
        <span class="sr-only">Cargando...</span>
    </div>
</div>
<!-- Spinner End -->

<%- include('../../partials/navbar') %>

<div class="container py-5 text-center">
    <h1 class="mt-2">Área del Usuario</h1>
    <p class="text-muted pt-1">Administrá tus accesos y datos dentro del sistema.</p>
    <% if (usuario) { %>
        <div class="d-flex justify-content-center mb-4">
          <div class="card shadow-sm border-0" style="max-width: 600px; width: 100%;">
            <div class="card-body">
              <div class="d-flex justify-content-between align-items-center">

                <div>
                  <h5 class="mb-1">👋 ¡Bienvenido/a, <strong><%= usuario.nombre %></strong>!</h5>
                  <p class="mb-0 text-muted" style="font-size: 0.9rem;">Usuario registrado</p>
                    <p class="mb-0 text-muted" style="font-size: 0.85rem;"><i class="fas fa-envelope"></i> <%= usuario.email %></p>
                </div>
                  <a href="/auth/buscar" class="btn btn-outline-info btn-sm me-2">🔍 Buscar Cursos</a>
                  <a href="/auth/logout" class="btn btn-danger btn-sm me-2">
                      <i class="fas fa-sign-out-alt"></i> Cerrar sesión
                  </a>
                  <a href="/team" class="btn btn-outline-primary btn-sm">
                      <i class="fas fa-users"></i> Nuestro Equipo
                  </a>
              </div>
            </div>
          </div>
        </div>
      <% } %>
        <% if (rol === 'profesor') { %>
            <div class="container mt-5">
                <h2 class="mb-4">📚 Mis cursos asignados</h2>

                <% if (cursos.length === 0) { %>
                    <div class="alert alert-info">No estás asignado a ningún curso por el momento.</div>
                <% } else { %>
                    <div class="row justify-content-center">
                        <% cursos.forEach(curso => { %>
                            <div class="col-md-6 col-lg-4 mb-4">
                                <div class="card h-100 shadow-sm">
                                    <div class="card-body d-flex flex-column justify-content-between">
                                        <div>
                                            <h5 class="card-title"><i class="fas fa-book"></i> <%= curso.nombre %></h5>
                                            <p class="card-text text-muted" style="font-size: 0.9rem;"><%= curso.descripcion %></p>
                                        </div>
                                        <a href="/auth/curso/<%= curso.id_curso %>" class="btn btn-outline-primary mt-3">
                                            Ver curso <i class="fas fa-arrow-right"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
        <% } %>
        <% if (rol === 'alumno') { %>
        <div class="container mt-5">
            <h2 class="mb-4">🎓 Mis cursos inscriptos</h2>

            <% if (cursosAlumno.length === 0) { %>
                <div class="alert alert-warning">No estás inscripto en ningún curso aún.</div>
            <% } else { %>
                <div class="row justify-content-center">
                    <% cursosAlumno.forEach(curso => { %>
                        <div class="col-md-6 col-lg-4 mb-4">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body d-flex flex-column justify-content-between">
                                    <div>
                                        <h5 class="card-title"><i class="fas fa-book-open"></i> <%= curso.nombre %></h5>
                                        <p class="card-text text-muted" style="font-size: 0.9rem;"><%= curso.descripcion %></p>
                                    </div>
                                    <a href="/auth/curso/<%= curso.id_curso %>" class="btn btn-outline-success mt-3">
                                        Ver curso <i class="fas fa-arrow-right"></i>
                                    </a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                </div>
            <% } %>
        </div>
    <% } %>


</div>

<%- include('../../partials/footer') %>

<!-- Back to Top -->
<a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>

<!-- JS Libraries -->
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
<script src="/lib/wow/wow.min.js"></script>
<script src="/lib/easing/easing.min.js"></script>
<script src="/lib/waypoints/waypoints.min.js"></script>
<script src="/lib/owlcarousel/owl.carousel.min.js"></script>

<!-- Template JS -->
<script src="/js/main.js"></script>
</body>
</html>
